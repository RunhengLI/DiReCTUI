<UserControl x:Class="DiReCTUI.Views.Components.TextBlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <TextBlock Name="Block" TextAlignment="Center" Background="{Binding BlockColor,UpdateSourceTrigger=PropertyChanged}" Width="195" Height="135" Margin="0 0 0 5" Text="{Binding BlockText,UpdateSourceTrigger=PropertyChanged}">
        <TextBlock.InputBindings>
            <MouseBinding Command="{Binding BlockClick}" MouseAction="LeftClick" />
        </TextBlock.InputBindings>
    </TextBlock>
</UserControl>
