<UserControl Name="SelectionViewControl" x:Class="DiReCTUI.Views.SelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="420" d:DesignWidth="625">
    <Grid>

        <ContentControl Content="{Binding Content,UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ContentVisibility,UpdateSourceTrigger=PropertyChanged}" />

        <ScrollViewer Content="{Binding SelectionGrid,UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding SelectionGridVisibility,UpdateSourceTrigger=PropertyChanged}" />

    </Grid>
    
</UserControl>
